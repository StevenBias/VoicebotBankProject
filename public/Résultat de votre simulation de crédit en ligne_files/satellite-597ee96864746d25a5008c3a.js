var source = source || {};
  source.env = "DTM";
if(s.cookieRead("55_visitStarted") === "page1"){
  		source.campaign = s.cookieRead('55_CurrentTrafficSource');
    	source.cts = s.cookieRead('55_CurrentTrafficSource');
			source.pts =s.cookieRead('55_lastPrioSource');  
}
